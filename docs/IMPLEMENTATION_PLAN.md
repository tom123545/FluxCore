# FluxCore 通用审批服务平台实施记录

> 本文件是后续模型接手时的工作基线。每完成一个阶段，都要更新“当前进度”和“验收记录”，不要重新设计已经确认的范围。

## 1. 项目目标

在 48～72 小时内交付一个可本地运行、可复现、可讲解的通用审批服务平台，完成从业务申请、审批流转、待办处理到最终通过/驳回的完整链路。

## 2. 已确认技术决策

- Java 21 + Spring Boot 3.4.x。
- Maven 多模块工程。
- 只有一个通用 `approval-service` 审批引擎；采购和合同统一由 `business-service` 提供业务数据，不得各自复制审批服务，新增审批流程只增加流程配置和必要的业务适配器。
- MySQL 8：使用一个 `fluxcore` 数据库和按职责划分的多张表；服务通过代码/接口边界隔离数据，不直接跨服务 Join。
- Redis 7：用于提交和审批动作的分布式锁；不把 Redis 锁作为唯一一致性保障。
- RabbitMQ：异步通知和领域事件；不引入 RocketMQ，避免重复建设消息基础设施。
- API 优先，Swagger 作为第一版验证入口；前端页面不是第一优先级。
- 不使用真实业务数据、生产敏感信息或外部 AI API Key。
- 审批历史必须读取审批时保存的数据快照，而不是当前业务表。

## 3. 服务边界

| 服务 | 端口 | 目标 |
|---|---:|---|
| gateway-service | 8080 | 统一入口和路由 |
| approval-service | 8081 | 唯一通用审批引擎：流程定义、审批实例、审批动作、待办、历史、快照 |
| business-service | 8082 | 采购、合同及未来业务申请数据，不包含审批引擎 |
| notification-service | 8084 | 消费审批事件并模拟通知 |

## 4. 分阶段任务

### Phase 0：工程基础（当前阶段）

- [x] 创建 Maven 父工程和四个应用服务模块。
- [x] 创建 MySQL、Redis、RabbitMQ 的 Docker Compose 基础设施。
- [x] 创建单库初始化脚本（`fluxcore`）。
- [x] 创建服务启动类和基础配置。
- [x] 创建本实施记录、架构说明和 README。
- [x] 通过 IDEA Maven Runner 完成根工程首次 `compile`，构建结果为 `BUILD SUCCESS`。
- [x] 使用本机 Maven 仓库中的依赖，通过 `javac` 编译全部 10 个 Java 源文件。
- [x] 将业务服务和审批服务的数据访问层切换为 MyBatis-Plus；Mapper 使用 `@Mapper` + `BaseMapper`，Entity 使用 `@TableName` + `@TableId`。

### Phase 1：审批领域核心

- [x] 定义审批流程、流程版本、节点、流转规则。
- [x] 定义审批实例、审批动作、审批快照、待办、幂等字段、Outbox 事件。
- [x] 实现配置化串行审批流程的首节点创建。
- [x] 实现提交审批、审批通过、审批驳回和审批撤回；动作均由 `approval-service` 统一承载。
- [x] 支持审批节点 `SINGLE` 和 `OR`（或签）模式；`AND`（并签/会签）暂不实现。
- [x] 实现串行审批基础状态机；统一校验审批实例、节点实例和审批任务的合法状态转换。
- [x] 实现审批实例查询，返回实例摘要、当前节点和任务处理状态。
- [x] 实现审批历史和快照查询。

### Phase 2：业务接入

- [x] 在 `business-service` 中实现采购申请和合同变更业务。
- [x] 配置采购三级审批流程。
- [x] 配置合同两级审批流程。
- [x] 通过 `business-service` 的内部业务数据接口读取业务数据，不在审批服务中复制业务流程；新增业务类型不新增审批服务。

### Phase 3：待办和标准动作

- [x] 审批节点到达时创建首个待办。
- [x] 审批完成、驳回或撤回时清理/终结待办。
- [x] 实现待办/已办查询。
- [x] 实现转审。
- [x] 实现加签。
- [x] 明确审批委托本期不实现，不新增委托接口和数据模型。

### Phase 4：可靠性

- [x] 使用 Redis 分布式锁保护重复提交。
- [x] 使用 Redis 分布式锁保护同一审批实例的并发动作。
- [x] 使用申请/审批业务表中的幂等字段和 MySQL 唯一索引实现最终幂等保障。
- [x] 为提交审批、审批通过/驳回/撤回、重复动作和 Redis 锁冲突补充单元测试。
- [x] 编写 `docs/MODEL_HANDOFF.md`，记录当前实际状态、未完成任务、并行开发边界和后续验收清单。
- [x] 使用审批实例 `lock_version` 字段实现乐观锁更新。
- [x] 使用 Outbox + RabbitMQ 实现异步通知。
- [x] Outbox 发布失败按 `retry_count/next_retry_at` 重试；通知消费按 `event_id` 去重并依赖 RabbitMQ 重投，不阻断审批主流程。

### Phase 5：验证和交付

- [x] 单元测试：串行状态机、权限、幂等和快照基础行为。
- [x] 真实 HTTP 验证：采购三级通过、开始即驳回、最后一级驳回、开始即撤回和最终批准前撤回。
- [ ] 专项测试：并发审批和或签聚合规则。
- [x] 集成测试：采购三级、合同两级完整审批链路。
- [ ] 编写 Swagger 示例和 PowerShell 演示脚本。
- [ ] 完善架构图、数据模型、设计取舍和启动说明。
- [ ] 从干净环境验证一键启动。

## 5. 最终验收场景

1. 启动基础设施和全部服务。
2. 创建采购申请，自动进入第一级待办。
3. 依次完成采购三级审批，状态变为 APPROVED。
4. 查询历史，确认每一级都能看到当时的数据快照。
5. 创建合同变更，验证其使用两级流程。
6. 使用相同幂等键重复提交，返回原审批实例而不是创建新实例。
7. 两个请求同时审批同一实例，一个成功，一个返回冲突。
8. 停止通知服务，审批仍成功，消息可以重试。
9. 展示新增业务类型只需要在 `business-service` 增加业务模块和流程配置。

## 6. 当前进度

当前已完成申请创建、配置化流程匹配、提交审批首节点、审批通过/驳回/撤回动作，以及审批实例、待办、已办、历史、快照、转审和加签。审批动作现已接入串行审批基础状态机：审批实例只允许 `IN_PROGRESS -> APPROVED/REJECTED/WITHDRAWN`，任务只允许从 `PENDING` 转为处理结果，节点只允许从 `ACTIVE` 转为完成、驳回或取消；三层审批通过 `approval_transition` 推进当前节点，不把审批层级写死到状态枚举中。审批节点当前支持 `SINGLE` 和 `OR`（或签）：`OR` 任一待办通过后取消其余待办并推进；`AND`（并签/会签）暂不实现，运行时会拒绝该模式。审批动作统一使用实例 Redis 锁、动作幂等字段、任务条件更新和 `lock_version` 乐观锁；末级通过时通过 `business-service` 将统一申请及业务子表改为 `APPROVED`；撤回会终结待办和活动节点并同步为 `WITHDRAWN`，驳回会终结当前节点、取消其他待办并同步为 `REJECTED`。审批实例查询接口 `GET /api/approvals/{approvalInstanceId}` 返回实例摘要、业务标题、当前节点和全部任务状态；待办/已办接口按审批人查询 `PENDING` 与非 `PENDING` 任务，并返回任务、实例、节点和最新快照摘要；历史接口 `GET /api/approvals/{approvalInstanceId}/history` 按动作时间返回动作及其关联快照数据，快照接口 `GET /api/approvals/{approvalInstanceId}/snapshots` 按快照编号返回实例的不可变快照；转审接口将原任务置为 `TRANSFERRED` 并创建替代待办，加签接口在当前节点追加待办。Outbox 发布器按批次锁定可投递事件并发布统一事件信封，成功标记 `PUBLISHED`，失败写入重试时间；notification-service 监听 RabbitMQ，按 `event_id` 去重并记录模拟站内信。相关单元测试已通过（2026-09-03）；采购三级通过、开始/末级驳回、开始/末级批准前撤回和合同两级完整审批的真实 HTTP 链路已验证，并发专项和真实 RabbitMQ 消费链路仍待验证。下一步是并发/集成测试、Swagger 示例和交付验证。

源码验证证据：2026-09-01 使用 JDK 21、Maven 本地仓库中的 100 个依赖 JAR，通过 `javac -proc:none` 编译初始五模块骨架的 10 个 Java 源文件，生成 10 个 class 文件并通过。采购和合同已合并为 `business-service`，合并后的四模块工程已完成 POM、目录和 Compose 静态校验，待 IDEA Maven 再次执行 `compile` 确认。

### 当前环境记录（2026-09-01）

- JDK 21：可用。
- Docker 29.7.2：可用。
- Docker Compose v5.4.0：可用，`docker compose config --quiet` 已通过。
- Maven：IDEA Maven Runner 已成功完成根工程 `compile`；当前 Codex PowerShell 中仍未识别 `mvn`，命令行 PATH 可后续单独修复。
- Docker：用户已成功拉取并启动 MySQL、Redis、RabbitMQ 镜像和容器。

## 7. 后续模型执行规则

- 先阅读本文件、`docs/ARCHITECTURE.md` 和根目录 `README.md`。
- 先检查 Git 工作区，不覆盖已有用户修改。
- 按 Phase 顺序执行，优先完成未勾选的最早任务。
- 每次完成一组任务后更新本文件的复选框、当前进度和验收记录。
- 未经用户明确要求，不引入 RocketMQ、Kubernetes、复杂 BPMN 引擎或真实生产配置。
